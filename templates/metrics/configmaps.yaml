{{/* These raw templates are the metrics profile's default implementation - superseded by the optional monitoring-logging dependency (templates from the "monitoring-logging" Chart.yaml dependency) when monitoringLogging.enabled is true, to avoid running two Grafanas/Tempos/otel-collectors at once. */}}
{{- if and .Values.metrics.enabled (not .Values.monitoringLogging.enabled) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-collector-config
  labels:
    {{- include "podiumd-minikube.labels" . | nindent 4 }}
data:
  otel-collector.yaml: {{ .Files.Get "vendor/dimpact-zaakafhandelcomponent/metrics/otel-collector.yaml" | quote }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: tempo-config
  labels:
    {{- include "podiumd-minikube.labels" . | nindent 4 }}
data:
  tempo.yaml: {{ .Files.Get "vendor/dimpact-zaakafhandelcomponent/metrics/tempo.yaml" | quote }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  labels:
    {{- include "podiumd-minikube.labels" . | nindent 4 }}
data:
  prometheus.yaml: {{ .Files.Get "vendor/dimpact-zaakafhandelcomponent/metrics/prometheus.yaml" | quote }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  labels:
    {{- include "podiumd-minikube.labels" . | nindent 4 }}
data:
  grafana-datasources.yaml: {{ .Files.Get "vendor/dimpact-zaakafhandelcomponent/metrics/grafana-datasources.yaml" | quote }}
{{- end }}
